
@{
    ViewBag.Title = "Index";
}
<!--
<ul class="nav nav-tabs" id="myTab">
    <li class="nav-item active">
        @*<a class="nav-link " href="#home" data-toggle="tab">教师信息</a>*@
        @Ajax.ActionLink("教师信息", "Info",null,
       new AjaxOptions { UpdateTargetId = "home", InsertionMode = InsertionMode.Replace, HttpMethod = "Get" },
       new { @class = "nav-link " })
    </li>
    <li class="nav-item">
        @*<a class="nav-link" href="#jmeter" data-toggle="tab">编辑教师信息</a>*@
        @Ajax.ActionLink("编辑教师信息", "Edit",null,
       new AjaxOptions { UpdateTargetId = "home", InsertionMode = InsertionMode.Replace, HttpMethod = "Get" },
       new { @class = "nav-link " })
    </li>
    <li class="nav-item">
        @*<a class="nav-link" href="#ejb" data-toggle="tab">新增教师信息</a>*@
        @Ajax.ActionLink("新增教师信息", "Add",null,
       new AjaxOptions { UpdateTargetId = "home", InsertionMode = InsertionMode.Replace, HttpMethod = "Get" },
       new { @class = "nav-link " })
    </li>

</ul>

<div id="home" class="tab-content">
    @*<div class="tab-pane fade in active" id="home">
    </div>*@
</div>
-->
<ul class="nav nav-tabs" id="myTab" role="tablist">
    <li class="nav-item">
        <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true">教师信息</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" id="profile-tab" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false">添加教师</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" id="contact-tab" data-toggle="tab" href="#contact" role="tab" aria-controls="contact" aria-selected="false">修改教师</a>
    </li>
</ul>
<div class="tab-content" id="myTabContent">
    <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">@Html.Action("Info")</div>
    <div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="profile-tab">...</div>
    <div class="tab-pane fade" id="contact" role="tabpanel" aria-labelledby="contact-tab">...</div>
</div>
@section scripts{
    <script src="~/Scripts/jquery.unobtrusive-ajax.js"></script>
    <script>
        //$("#home-tab").click(function () {
        //    $("#home").empty()
        //    $.ajax({
        //        url: "/TeaInfo/Info",
        //        type: "get",
        //        success: function (data) {
        //            console.log(data)
        //            $("#home").html(data)
        //        }

        //    })

        })

    </script>

}
